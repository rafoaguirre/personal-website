---
import { getCollection } from 'astro:content';
import { siGithub } from 'simple-icons';
import ArticleCard from '../components/ArticleCard.astro';
import ProjectCard from '../components/ProjectCard.astro';
import TechMarquee from '../components/TechMarquee.astro';
import BaseLayout from '../layouts/BaseLayout.astro';

const linkedinIconPath =
  'M20.447 20.452H16.89v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.345V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 1 1 0-4.124 2.062 2.062 0 0 1 0 4.124zm1.777 13.019H3.56V9h3.554v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z';

const articles = (await getCollection('articles', ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishedAt.valueOf() - a.data.publishedAt.valueOf())
  .slice(0, 3);

const projects = (await getCollection('projects'))
  .filter((project) => project.data.featured)
  .sort((a, b) => b.data.year - a.data.year)
  .slice(0, 3);

const workStats = [
  { label: 'Years in production engineering', value: '8+' },
  { label: 'Primary ecosystems', value: 'EVM + Solana' },
  { label: 'Delivery range', value: 'web, mobile, backend, cloud' },
];

const workNotes = [
  'blockchain architecture',
  'backend and platform systems',
  'web and mobile product delivery',
  'technical writing and documentation',
];

type ContractorProject = {
  name: string;
  href: string;
  label: string;
  image?: string;
  fallbackTag?: string;
  fallbackSummary?: string;
};

const focusAreas = [
  {
    title: 'Blockchain architecture',
    detail:
      'Smart contracts, DApps, wallets, NFT systems, cross-chain flows, and production-grade Web3 integration.',
  },
  {
    title: 'Backend and distributed systems',
    detail:
      'Event-driven services, operator tooling, APIs, data pipelines, and resilient infrastructure behind product teams.',
  },
  {
    title: 'Platform and delivery leadership',
    detail:
      'CI/CD, SRE, Terraform, cloud architecture, client communication, and technical guidance from design through release.',
  },
];

const experience = [
  {
    role: 'Founder / Senior Full-Stack & Blockchain Developer',
    company: 'EastCode Blockchain Labs',
    period: '2017 - Present',
    location: 'Canada',
    points: [
      'Architect and deliver custom web, mobile, and blockchain systems for client engagements and internal products.',
      'Lead teams and builds across Node.js, Rust, Golang, PostgreSQL, MongoDB, cloud infrastructure, and CI/CD pipelines.',
    ],
  },
  {
    role: 'Infrastructure Engineer',
    company: 'ATMTA Inc.',
    period: '2021 - 2023',
    location: 'Remote',
    points: [
      'Supported core infrastructure for Star Atlas and related Web3 platform services built on Solana.',
      'Automated build and deployment workflows to improve release consistency and operational efficiency.',
    ],
  },
  {
    role: 'Development Lead',
    company: 'Multichain Ventures',
    period: '2018 - 2020',
    location: 'Remote',
    points: [
      'Contributed to a React Native mobile wallet supporting multi-chain transactions and token management.',
      'Owned internal tooling, shared npm packages, DevOps pipelines, and site reliability across the engineering team.',
    ],
  },
  {
    role: 'Blockchain Developer',
    company: 'Tokes Platform / Sigwo Technologies',
    period: '2017 - 2020',
    location: 'Remote',
    points: [
      'Built payment, block-explorer, and chain-integration systems across Waves, Nxt, and custom blockchain environments.',
      'Implemented bridge and decentralized database concepts for cross-chain and distributed application use cases.',
    ],
  },
];

const contractorProjects: ContractorProject[] = [
  {
    name: 'Pentos',
    href: 'https://www.pentos.ai',
    label: 'AI collaboration platform',
    image: '/project-previews/pentos-homepage-preview.png',
  },
  {
    name: 'Claynosaurz',
    href: 'https://collect.claynosaurz.com/',
    label: 'Collectibles / badges experience',
    image: '/project-previews/claynosaurz-homepage-preview.png',
  },
  {
    name: 'SpiritSwap',
    href: 'https://spiritswap.finance/',
    label: 'DeFi / DEX interface',
    image: '/project-previews/spiritswap-homepage-preview.png',
  },
  {
    name: 'Star Atlas',
    href: 'https://staratlas.com/',
    label: 'Web3 game',
    image: '/project-previews/star-atlas-homepage-preview.png',
  },
  {
    name: 'Nymble',
    href: 'https://nymble.world/',
    label: 'Nostr social client',
    image: '/project-previews/nymble-homepage-preview.png',
  },
  {
    name: 'Tokes Platform',
    href: 'https://multichain.ventures/tokes.html',
    label: 'Blockchain payments',
    image: '/project-previews/tokes-homepage-preview.png',
  },
];
---

<BaseLayout
  title="Rafael Aguirre"
  description="Blockchain architect and senior full-stack engineer building production systems across Web3, backend, and cloud."
>
  <section class="grid gap-8 pb-10 pt-6 lg:grid-cols-[1.15fr_0.85fr] lg:items-stretch lg:gap-10">
    <div class="panel-card relative overflow-hidden rounded-[2.2rem] p-8 sm:p-10 lg:p-12">
      <div
        class="absolute inset-0 bg-[radial-gradient(circle_at_top_left,rgba(232,95,59,0.18),transparent_28rem),radial-gradient(circle_at_90%_30%,rgba(20,117,122,0.15),transparent_22rem)]"
      >
      </div>
      <div class="comic-dots absolute inset-0 opacity-35"></div>
      <div class="relative">
        <p class="section-kicker">blockchain architect / senior engineer</p>
        <p class="mt-6 text-sm font-medium uppercase tracking-[0.18em] text-[var(--site-muted)]">
          Rafael Aguirre
        </p>
        <h1
          class="mt-2 max-w-3xl text-3xl leading-tight font-semibold text-[var(--site-ink)] sm:text-4xl lg:text-5xl"
        >
          Blockchain architecture, full-stack delivery, and systems built to hold up in production.
        </h1>
        <p class="mt-6 max-w-2xl text-lg leading-8 text-[var(--site-muted)] sm:text-xl">
          I work across EVM, Solana, backend platforms, mobile products, and cloud infrastructure,
          with an emphasis on reliability, architectural clarity, and shipping cleanly under
          real-world constraints.
        </p>
        <div class="mt-8 flex flex-wrap gap-3">
          <a
            href="/projects"
            class="rounded-full border border-[var(--site-ink)] bg-[var(--site-ink)] px-6 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-bg-soft)] hover:-translate-y-0.5 hover:bg-[var(--site-accent)]"
          >
            See projects
          </a>
          <a
            href="/articles"
            class="rounded-full border border-[var(--site-line)] bg-[var(--site-panel)] px-6 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-ink)] hover:-translate-y-0.5 hover:border-[var(--site-accent)]"
          >
            Read articles
          </a>
        </div>
      </div>
    </div>

    <div class="grid gap-4">
      <div class="panel-card site-grid relative overflow-hidden rounded-[2rem] p-6 sm:p-8">
        <div
          class="absolute -right-8 top-8 h-28 w-28 rounded-full border-2 border-[var(--site-ink)] bg-[var(--site-yellow)]"
        >
        </div>
        <div class="relative pt-10">
          <h2 class="max-w-md text-3xl font-semibold text-[var(--site-ink)] sm:text-4xl">
            What I work on most.
          </h2>
          <p class="mt-4 max-w-lg text-base leading-7 text-[var(--site-muted)]">
            The overlap between architecture, shipping, and long-term maintainability is where I do
            my best work.
          </p>
          <div class="mt-8 flex flex-wrap gap-2">
            {
              workNotes.map((note) => (
                <span class="rounded-full border border-[var(--site-line)] bg-[var(--site-panel-strong)] px-3 py-1.5 text-xs font-semibold uppercase tracking-[0.16em] text-[var(--site-muted)]">
                  {note}
                </span>
              ))
            }
          </div>
        </div>
      </div>
      <div class="panel-card rounded-[2rem] p-6">
        <p
          class="text-[0.72rem] font-semibold uppercase tracking-[0.18em] text-[var(--site-muted)]"
        >
          current focus
        </p>
        <p class="mt-3 text-base leading-7 text-[var(--site-muted)]">
          Smart contracts, backend services, platform infrastructure, typed abstractions, and the
          places where product ideas meet operational reality.
        </p>
        <div class="mt-6 grid gap-3 sm:grid-cols-3 lg:grid-cols-1 xl:grid-cols-3">
          {
            workStats.map((stat) => (
              <div class="rounded-[1.4rem] border border-[var(--site-line)] bg-[var(--site-panel-strong)] px-4 py-4">
                <p class="text-2xl font-semibold text-[var(--site-ink)]">{stat.value}</p>
                <p class="mt-2 text-sm leading-6 text-[var(--site-muted)]">{stat.label}</p>
              </div>
            ))
          }
        </div>
        <div class="mt-6 grid gap-3 sm:grid-cols-2">
          <a
            href="https://linkedin.com/in/rafaeloaguirre"
            target="_blank"
            rel="noopener noreferrer"
            class="flex min-h-24 items-center justify-center rounded-[1.4rem] border border-[var(--site-line)] bg-[var(--site-panel)] p-4 hover:-translate-y-0.5"
            aria-label="LinkedIn"
          >
            <span
              class="flex h-14 w-14 items-center justify-center rounded-full border border-[var(--site-line)] bg-[var(--site-panel-strong)] text-[#0A66C2]"
            >
              <svg viewBox="0 0 24 24" class="h-7 w-7 fill-current" aria-hidden="true">
                <path d={linkedinIconPath}></path>
              </svg>
            </span>
          </a>
          <a
            href="https://github.com/rafoaguirre"
            target="_blank"
            rel="noopener noreferrer"
            class="flex min-h-24 items-center justify-center rounded-[1.4rem] border border-[var(--site-line)] bg-[var(--site-panel)] p-4 hover:-translate-y-0.5"
            aria-label="GitHub"
          >
            <span
              class="flex h-14 w-14 items-center justify-center rounded-full border border-[var(--site-line)] bg-[var(--site-panel-strong)] text-[var(--site-ink)]"
            >
              <svg viewBox="0 0 24 24" class="h-7 w-7 fill-current" aria-hidden="true">
                <path d={siGithub.path}></path>
              </svg>
            </span>
          </a>
        </div>
      </div>
    </div>
  </section>

  <section class="pb-16">
    <TechMarquee />
  </section>

  <section id="about" class="border-t border-[var(--site-line)] py-20">
    <div class="mb-8 max-w-4xl">
      <p class="section-kicker">about me</p>
      <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
        I like hard systems problems, readable architecture, and products that actually ship.
      </h2>
    </div>
    <div class="panel-card rounded-[1.7rem] p-6 sm:p-7">
      <p class="text-base leading-8 text-[var(--site-muted)]">
        I have spent the last several years working across blockchain architecture, backend
        engineering, mobile products, and cloud infrastructure. My strongest lane is the
        intersection of hands-on engineering and system design: translating ambiguous requirements
        into production-ready software.
      </p>
      <p class="mt-4 text-base leading-8 text-[var(--site-muted)]">
        Most of my recent work sits across EVM and Solana ecosystems, but the broader pattern is
        consistent: distributed applications, event-driven flows, platform reliability, and delivery
        standards that hold up after launch.
      </p>
    </div>
    <div class="mt-5 grid gap-4 md:grid-cols-3">
      {
        focusAreas.map((area) => (
          <article class="panel-card rounded-[1.5rem] p-5">
            <h3 class="text-xl font-semibold text-[var(--site-ink)]">{area.title}</h3>
            <p class="mt-3 text-sm leading-6 text-[var(--site-muted)]">{area.detail}</p>
          </article>
        ))
      }
    </div>
  </section>

  <section id="experience" class="border-t border-[var(--site-line)] py-20">
    <div class="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
      <div>
        <p class="section-kicker">experience</p>
        <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
          Built through client work, startups, and infrastructure-heavy teams.
        </h2>
      </div>
      <p class="max-w-xl text-sm leading-7 text-[var(--site-muted)]">
        The common thread is owning the technical path from idea to deployment, then staying close
        enough to operations to know whether the system is actually doing its job.
      </p>
    </div>

    <div class="grid gap-5 lg:grid-cols-2">
      {
        experience.map((item) => (
          <article class="panel-card rounded-[1.7rem] p-6">
            <div class="flex flex-col gap-3 sm:min-h-[5.5rem] sm:flex-row sm:items-start sm:justify-between">
              <div>
                <p class="text-[0.72rem] font-semibold uppercase tracking-[0.18em] text-[var(--site-muted)]">
                  {item.company}
                </p>
                <h3 class="mt-2 text-2xl font-semibold text-[var(--site-ink)]">{item.role}</h3>
              </div>
              <div class="min-w-[9.5rem] shrink-0 rounded-2xl border border-[var(--site-line)] px-4 py-2 text-center text-sm text-[var(--site-muted)]">
                <p>{item.period}</p>
                <p>{item.location}</p>
              </div>
            </div>
            <ul class="mt-5 space-y-3 text-sm leading-7 text-[var(--site-muted)]">
              {item.points.map((point) => (
                <li class="flex gap-3">
                  <span class="mt-2 h-2.5 w-2.5 shrink-0 rounded-full bg-[var(--site-accent)]" />
                  <span>{point}</span>
                </li>
              ))}
            </ul>
          </article>
        ))
      }
    </div>
  </section>

  <section id="contract-work" class="border-t border-[var(--site-line)] py-20">
    <div class="mb-8 flex flex-col gap-4 md:flex-row md:items-end md:justify-between">
      <div>
        <p class="section-kicker">contract work</p>
        <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
          Products and platforms I have contributed to with client and team partners.
        </h2>
      </div>
      <p class="max-w-xl text-sm leading-7 text-[var(--site-muted)]">
        These are public projects where my role was to contribute engineering, architecture,
        infrastructure, or product delivery work.
      </p>
    </div>

    <div class="flex flex-wrap justify-center gap-5">
      {
        contractorProjects.map((project) => (
          <a
            href={project.href}
            target="_blank"
            rel="noopener noreferrer"
            class="panel-card group w-full max-w-[24rem] overflow-hidden rounded-[1.7rem] hover:-translate-y-1"
          >
            {project.image ? (
              <div class="aspect-[16/10] overflow-hidden border-b border-[var(--site-line)] bg-[var(--site-bg-soft)]">
                <img
                  src={project.image}
                  alt={`${project.name} site preview`}
                  loading="lazy"
                  class="h-full w-full object-cover object-top transition-transform duration-300 group-hover:scale-[1.02]"
                />
              </div>
            ) : (
              <div class="relative flex aspect-[16/10] items-end overflow-hidden border-b border-[var(--site-line)] bg-[linear-gradient(135deg,#0f1117,#1a2030_48%,#11424a)] p-6">
                <div class="absolute left-5 top-5 rounded-full border border-white/14 bg-white/8 px-3 py-1 text-[0.68rem] font-semibold uppercase tracking-[0.16em] text-white/70">
                  live product
                </div>
                <div class="absolute -right-8 top-8 h-28 w-28 rounded-full border border-white/10 bg-[radial-gradient(circle_at_center,rgba(255,255,255,0.28),rgba(255,255,255,0.02))]" />
                <div class="relative">
                  <p class="text-[0.72rem] font-semibold uppercase tracking-[0.18em] text-white/60">
                    {project.fallbackTag ?? project.name}
                  </p>
                  <p class="mt-2 max-w-[14rem] text-2xl font-semibold leading-tight text-white">
                    {project.fallbackSummary ?? project.label}
                  </p>
                </div>
              </div>
            )}
            <div class="p-6">
              <p class="text-[0.72rem] font-semibold uppercase tracking-[0.18em] text-[var(--site-muted)]">
                {project.label}
              </p>
              <h3 class="mt-3 text-2xl font-semibold text-[var(--site-ink)] group-hover:text-[var(--site-accent)]">
                {project.name}
              </h3>
            </div>
          </a>
        ))
      }
    </div>
  </section>

  {
    projects.length > 0 && (
      <section id="portfolio" class="border-t border-[var(--site-line)] py-20">
        <div class="mb-8 flex items-end justify-between gap-4">
          <div>
            <p class="section-kicker">portfolio</p>
            <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
              Things I am shipping in public.
            </h2>
          </div>
          <a
            href="/projects"
            class="text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-muted)] hover:text-[var(--site-accent)]"
          >
            All projects
          </a>
        </div>

        <div class="grid gap-5 lg:grid-cols-3">
          {projects.map((project) => (
            <ProjectCard
              id={project.id}
              title={project.data.title}
              summary={project.data.summary}
              stack={project.data.stack}
              year={project.data.year}
              repoUrl={project.data.repoUrl}
              liveUrl={project.data.liveUrl}
            />
          ))}
        </div>
      </section>
    )
  }

  {
    articles.length > 0 && (
      <section id="articles" class="border-t border-[var(--site-line)] py-20">
        <div class="mb-8 flex items-end justify-between gap-4">
          <div>
            <p class="section-kicker">articles</p>
            <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
              Writing about architecture, trade-offs, and what holds up in practice.
            </h2>
          </div>
          <a
            href="/articles"
            class="text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-muted)] hover:text-[var(--site-accent)]"
          >
            All writing
          </a>
        </div>

        <div class="grid gap-5">
          {articles.map((article) => (
            <ArticleCard
              id={article.id}
              title={article.data.title}
              description={article.data.description}
              publishedAt={article.data.publishedAt}
              tags={article.data.tags}
            />
          ))}
        </div>
      </section>
    )
  }

  <section class="border-t border-[var(--site-line)] py-20">
    <div
      class="panel-card grid gap-6 rounded-[2rem] p-8 lg:grid-cols-[1.1fr_0.9fr] lg:items-center lg:p-10"
    >
      <div>
        <p class="section-kicker">let's build</p>
        <h2 class="mt-5 text-4xl font-semibold text-[var(--site-ink)]">
          Open to interesting systems work, architecture-heavy builds, and thoughtful product
          collaborations.
        </h2>
      </div>
      <div class="space-y-4">
        <p class="text-base leading-7 text-[var(--site-muted)]">
          If you are building something that needs calm execution across blockchain, backend, or
          platform infrastructure, there is a good chance I can help.
        </p>
        <div class="flex flex-wrap gap-3">
          <a
            href="/contact"
            class="rounded-full border border-[var(--site-ink)] bg-[var(--site-ink)] px-6 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-bg-soft)] hover:-translate-y-0.5 hover:bg-[var(--site-accent)]"
          >
            Get in touch
          </a>
          <a
            href="https://linkedin.com/in/rafaeloaguirre"
            target="_blank"
            rel="noopener noreferrer"
            class="rounded-full border border-[var(--site-line)] bg-[var(--site-panel-strong)] px-6 py-3 text-sm font-semibold uppercase tracking-[0.18em] text-[var(--site-ink)] hover:-translate-y-0.5 hover:border-[var(--site-accent)]"
          >
            LinkedIn
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>
